# doctor-profile-page
Doctor Profile Page

A clean, responsive single-page profile layout for medical professionals — perfect for showcasing a doctor’s photo, credentials, specialties, contact info, and more.

🧩 Features

Built with HTML5, SCSS/CSS, and a simple file structure (no frameworks required)

Responsive design — adapts well to desktop & mobile viewports

Uses SCSS for organized styling with variables/mixins (see style.scss)

Includes an icons folder (under /icons) for easy vector or SVG assets

Minimal, clean aesthetic focused on readability and professional presentation

📁 Project Structure
/doctor-profile-page
│
├─ icons/              ← SVG or icon assets used in the layout  
├─ index.html          ← Entry point / profile page markup  
├─ style.scss          ← SCSS source for styling  
├─ style.css           ← Compiled CSS output  
└─ style.css.map       ← Source map for debugging CSS 